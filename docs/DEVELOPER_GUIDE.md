# 🛠️ Developer Guide | ডেভেলপার গাইড

এই ডকুমেন্টে artistiya.store ওয়েব অ্যাপ্লিকেশনের সম্পূর্ণ আর্কিটেকচার, ফিচার লিস্ট এবং ডেভেলপমেন্ট গাইডলাইন দেওয়া হয়েছে।

---

## 📋 সূচিপত্র

1. [প্রযুক্তি স্ট্যাক](#প্রযুক্তি-স্ট্যাক)
2. [প্রজেক্ট স্ট্রাকচার](#প্রজেক্ট-স্ট্রাকচার)
3. [সম্পূর্ণ ফিচার লিস্ট](#সম্পূর্ণ-ফিচার-লিস্ট)
4. [ডাটাবেস আর্কিটেকচার](#ডাটাবেস-আর্কিটেকচার)
5. [অথেন্টিকেশন সিস্টেম](#অথেন্টিকেশন-সিস্টেম)
6. [রিয়েল-টাইম সিঙ্ক](#রিয়েল-টাইম-সিঙ্ক)
7. [Edge Functions](#edge-functions)
8. [স্টাইলিং ও থিম](#স্টাইলিং-ও-থিম)
9. [লোকালাইজেশন](#লোকালাইজেশন)
10. [ডিপ্লয়মেন্ট](#ডিপ্লয়মেন্ট)

---

## প্রযুক্তি স্ট্যাক

| ক্যাটাগরি | প্রযুক্তি |
|-----------|----------|
| **Frontend** | React 18, TypeScript, Vite |
| **Styling** | Tailwind CSS, shadcn/ui |
| **Animation** | Framer Motion |
| **State Management** | TanStack Query (React Query) |
| **Routing** | React Router DOM v6 |
| **Backend** | Supabase (Lovable Cloud) |
| **Database** | PostgreSQL (via Supabase) |
| **Auth** | Supabase Auth |
| **Storage** | Supabase Storage |
| **Realtime** | Supabase Realtime |
| **Edge Functions** | Deno (Supabase Edge Functions) |
| **Rich Text** | TipTap Editor |
| **Forms** | React Hook Form + Zod |
| **PWA** | vite-plugin-pwa |

---

## প্রজেক্ট স্ট্রাকচার

```
src/
├── components/
│   ├── admin/           # অ্যাডমিন ড্যাশবোর্ড কম্পোনেন্ট
│   │   ├── crm/         # CRM ড্যাশবোর্ড মডিউল
│   │   └── ...
│   ├── checkout/        # চেকআউট কম্পোনেন্ট
│   ├── common/          # শেয়ার্ড কম্পোনেন্ট
│   ├── content/         # CMS কন্টেন্ট
│   ├── home/            # হোমপেজ সেকশন
│   ├── layout/          # Header, Footer
│   ├── mobile/          # মোবাইল-স্পেসিফিক কম্পোনেন্ট
│   ├── modals/          # মোডাল কম্পোনেন্ট
│   ├── product/         # প্রোডাক্ট কম্পোনেন্ট
│   ├── search/          # সার্চ ফাংশনালিটি
│   ├── ui/              # shadcn/ui কম্পোনেন্ট
│   └── widgets/         # চ্যাট উইজেট ইত্যাদি
├── hooks/               # কাস্টম React Hooks
├── pages/               # রাউট পেজ
├── integrations/
│   └── supabase/        # Supabase ক্লায়েন্ট ও টাইপস
├── data/                # স্ট্যাটিক ডাটা
├── lib/                 # ইউটিলিটি ফাংশন
├── assets/              # স্ট্যাটিক অ্যাসেট
└── test/                # টেস্ট ফাইল

supabase/
├── functions/           # Edge Functions
│   ├── bkash-payment/   # bKash পেমেন্ট
│   ├── nagad-payment/   # Nagad পেমেন্ট
│   ├── send-order-email/# ইমেইল নোটিফিকেশন
│   ├── generate-invoice/# ইনভয়েস জেনারেশন
│   └── ...
└── config.toml          # Supabase কনফিগ

docs/
├── DATABASE_MIGRATION_GUIDE.md
├── DEVELOPER_GUIDE.md
├── MOBILE_LAYOUT_GUIDE.md
└── SECURITY_GUIDE.md
```

---

## সম্পূর্ণ ফিচার লিস্ট

### 🛒 ই-কমার্স ফিচার

| ফিচার | বর্ণনা | স্ট্যাটাস |
|--------|--------|----------|
| **প্রোডাক্ট ম্যানেজমেন্ট** | প্রোডাক্ট CRUD, ইমেজ গ্যালারি, ভ্যারিয়েন্ট | ✅ |
| **ক্যাটাগরি সিস্টেম** | মাল্টি-লেভেল ক্যাটাগরি, আইকন, ইমেজ | ✅ |
| **কালেকশন** | প্রোডাক্ট কালেকশন ম্যানেজমেন্ট | ✅ |
| **প্রোডাক্ট ভ্যারিয়েন্ট** | সাইজ, কালার, স্টক ম্যানেজমেন্ট | ✅ |
| **ইনভেন্টরি ট্র্যাকিং** | স্টক কোয়ান্টিটি, লো স্টক এলার্ট | ✅ |
| **প্রাইসিং** | রেগুলার প্রাইস, সেল প্রাইস, ডিসকাউন্ট | ✅ |
| **ডাইনামিক ডিসকাউন্ট ব্যাজ** | অটো ক্যালকুলেটেড % ব্যাজ | ✅ |
| **প্রোডাক্ট বান্ডল** | কম্বো অফার, বান্ডল প্রোডাক্ট | ✅ |
| **প্রোডাক্ট রিভিউ** | কাস্টমার রিভিউ ও রেটিং | ✅ |
| **উইশলিস্ট** | প্রোডাক্ট সেভ ফর লেটার | ✅ |
| **কার্ট সিস্টেম** | কার্ট CRUD, পার্সিস্ট্যান্স | ✅ |
| **কাস্টম অর্ডার** | কাস্টম ডিজাইন রিকোয়েস্ট | ✅ |
| **কাস্টমাইজেশন অনলি প্রোডাক্ট** | "Request Custom Order" বাটন, অ্যাডভান্স পেমেন্ট | ✅ |

### 📦 অর্ডার ম্যানেজমেন্ট

| ফিচার | বর্ণনা | স্ট্যাটাস |
|--------|--------|----------|
| **অর্ডার ক্রিয়েশন** | গেস্ট ও রেজিস্টার্ড কাস্টমার | ✅ |
| **অর্ডার স্ট্যাটাস** | Pending → Confirmed → Processing → Shipped → Delivered | ✅ |
| **অর্ডার ট্র্যাকিং** | অর্ডার নম্বর দিয়ে ট্র্যাক | ✅ |
| **ইনভয়েস জেনারেশন** | PDF ইনভয়েস, QR কোড | ✅ |
| **ডেলিভারি স্লিপ** | 100mm x 150mm প্রিন্টযোগ্য | ✅ |
| **অর্ডার ইমেইল** | অটোমেটিক নোটিফিকেশন | ✅ |
| **অর্ডার এক্সপোর্ট** | CSV, JSON, PDF রিপোর্ট | ✅ |
| **বাল্ক অ্যাকশন** | মাল্টি-সিলেক্ট, স্ট্যাটাস চেঞ্জ | ✅ |
| **রিটার্ন/রিফান্ড** | রিটার্ন রিকোয়েস্ট ম্যানেজমেন্ট | ✅ |

### 💳 পেমেন্ট সিস্টেম

| ফিচার | বর্ণনা | স্ট্যাটাস |
|--------|--------|----------|
| **bKash** | bKash পেমেন্ট গেটওয়ে | ✅ |
| **Nagad** | Nagad পেমেন্ট গেটওয়ে | ✅ |
| **COD** | ক্যাশ অন ডেলিভারি | ✅ |
| **প্রোমো কোড** | ডিসকাউন্ট কুপন সিস্টেম | ✅ |
| **ফ্রি শিপিং** | থ্রেশহোল্ড বেসড ফ্রি শিপিং | ✅ |
| **পেমেন্ট ট্র্যাকিং** | পার্টনার পেমেন্ট স্ট্যাটাস | ✅ |

### 🚚 ডেলিভারি সিস্টেম

| ফিচার | বর্ণনা | স্ট্যাটাস |
|--------|--------|----------|
| **ডেলিভারি জোন** | ডিভিশন/জেলা বেসড শিপিং | ✅ |
| **শিপিং ক্যালকুলেটর** | অটোমেটিক শিপিং কস্ট | ✅ |
| **ডেলিভারি পার্টনার** | মাল্টি-পার্টনার সাপোর্ট | ✅ |
| **পার্টনার পারফরম্যান্স** | সাকসেস রেট ট্র্যাকিং | ✅ |
| **অ্যাড্রেস ম্যানেজমেন্ট** | মাল্টিপল অ্যাড্রেস | ✅ |

### 👥 কাস্টমার ম্যানেজমেন্ট (CRM)

| ফিচার | বর্ণনা | স্ট্যাটাস |
|--------|--------|----------|
| **কাস্টমার প্রোফাইল** | অটো-সিঙ্ক ফ্রম অর্ডার | ✅ |
| **প্রিমিয়াম মেম্বারশিপ** | VIP কাস্টমার | ✅ |
| **কাস্টমার ডিসকাউন্ট** | ইন্ডিভিজুয়াল ডিসকাউন্ট % | ✅ |
| **লাইফটাইম ভ্যালু** | টোটাল অর্ডার ও স্পেন্ট | ✅ |
| **অ্যাবন্ডনড কার্ট** | রিকভারি ট্র্যাকিং | ✅ |
| **ব্লক কাস্টমার** | ফ্রড প্রোটেকশন | ✅ |
| **লিড ম্যানেজমেন্ট** | কাস্টমার লিডস | ✅ |
| **CRM ড্যাশবোর্ড** | অ্যানালিটিক্স ও রিপোর্ট | ✅ |
| **বাল্ক এক্সপোর্ট** | CSV, JSON, PDF | ✅ |

### 📊 অ্যাডমিন ড্যাশবোর্ড

| ফিচার | বর্ণনা | স্ট্যাটাস |
|--------|--------|----------|
| **ড্যাশবোর্ড হোম** | স্ট্যাটস ওভারভিউ | ✅ |
| **রিয়েল-টাইম আপডেট** | লাইভ ডাটা সিঙ্ক | ✅ |
| **অর্ডার ম্যানেজমেন্ট** | ফুল CRUD | ✅ |
| **প্রোডাক্ট ম্যানেজমেন্ট** | বাল্ক আপলোড/এক্সপোর্ট | ✅ |
| **মিডিয়া ম্যানেজার** | ইমেজ লাইব্রেরি | ✅ |
| **CRM ড্যাশবোর্ড** | বিজনেস অ্যানালিটিক্স | ✅ |

### 🎨 CMS (কন্টেন্ট ম্যানেজমেন্ট)

| ফিচার | বর্ণনা | স্ট্যাটাস |
|--------|--------|----------|
| **হিরো স্লাইডার** | ব্যানার ম্যানেজমেন্ট | ✅ |
| **হোমপেজ সেকশন** | ড্র্যাগ অ্যান্ড ড্রপ সেকশন | ✅ |
| **ফিচার্ড সেকশন** | কাস্টম ব্যানার | ✅ |
| **মেকিং সেকশন** | প্রসেস শোকেস | ✅ |
| **ইনস্টাগ্রাম ফিড** | ম্যানুয়াল পোস্ট | ✅ |
| **টেস্টিমোনিয়াল** | কাস্টমার রিভিউ | ✅ |
| **ব্লগ** | পোস্ট ও ক্যাটাগরি | ✅ |
| **FAQ** | প্রশ্নোত্তর ম্যানেজমেন্ট | ✅ |
| **গ্যালারি** | অ্যালবাম ও মিডিয়া | ✅ |
| **কন্টেন্ট পেজ** | স্ট্যাটিক পেজ এডিটর | ✅ |
| **অ্যানাউন্সমেন্ট বার** | শিডিউলড ব্যানার | ✅ |
| **ইউটিউব ভিডিও** | ভিডিও গ্যালারি | ✅ |
| **সার্টিফিকেশন** | ব্র্যান্ড সার্টিফিকেট | ✅ |

### ⚙️ সাইট সেটিংস

| ফিচার | বর্ণনা | স্ট্যাটাস |
|--------|--------|----------|
| **সাইট ব্র্যান্ডিং** | লোগো, কালার, ডেসক্রিপশন | ✅ |
| **থিম সেটিংস** | কালার স্কিম, ফন্ট | ✅ |
| **শপ সেটিংস** | প্রোডাক্ট গ্রিড, পেজিনেশন | ✅ |
| **চেকআউট সেটিংস** | পেমেন্ট অপশন, ফিল্ড | ✅ |
| **কাস্টমাইজেশন সেটিংস** | কাস্টম অর্ডার টগল, অ্যাডভান্স % | ✅ |
| **ইমেইল সেটিংস** | SMTP/Resend কনফিগ | ✅ |
| **ফ্রড সেটিংস** | গেস্ট চেকআউট, লিমিট | ✅ |
| **ফিল্টার সেটিংস** | শপ ফিল্টার কাস্টমাইজ | ✅ |
| **কারেন্সি সেটিংস** | মাল্টি-কারেন্সি | ✅ |
| **ফুটার লিংক** | ডাইনামিক ফুটার মেনু | ✅ |
| **সোশ্যাল লিংক** | সোশ্যাল মিডিয়া | ✅ |
| **মেনু ম্যানেজার** | নেভিগেশন মেনু | ✅ |
| **ইনভয়েস সেটিংস** | কোম্পানি ইনফো, লোগো | ✅ |
| **QR সেটিংস** | ইনভয়েস QR কোড | ✅ |

### 📧 মার্কেটিং

| ফিচার | বর্ণনা | স্ট্যাটাস |
|--------|--------|----------|
| **নিউজলেটার** | সাবস্ক্রিপশন ফর্ম | ✅ |
| **সাবস্ক্রাইবার লিস্ট** | এক্সপোর্ট ফাংশন | ✅ |
| **আপসেল অফার** | চেকআউট আপসেল | ✅ |
| **সাইনআপ ডিসকাউন্ট** | নতুন কাস্টমার অফার | ✅ |
| **গুগল ইন্টিগ্রেশন** | Analytics, Reviews | ✅ |

### 📱 মোবাইল ফিচার

| ফিচার | বর্ণনা | স্ট্যাটাস |
|--------|--------|----------|
| **রেসপন্সিভ ডিজাইন** | মোবাইল-ফার্স্ট UI | ✅ |
| **মোবাইল নেভিগেশন** | বটম নেভ, ড্রয়ার | ✅ |
| **মোবাইল হোম** | ক্যাটাগরি পিল, স্লাইডার | ✅ |
| **মোবাইল চেকআউট** | সিম্পলিফাইড ফ্লো | ✅ |
| **PWA সাপোর্ট** | ইনস্টলেবল অ্যাপ | ✅ |
| **হোয়াটসঅ্যাপ অর্ডার** | ডাইরেক্ট অর্ডার বাটন | ✅ |

### 🔐 সিকিউরিটি

| ফিচার | বর্ণনা | স্ট্যাটাস |
|--------|--------|----------|
| **Row Level Security** | টেবিল-লেভেল পলিসি | ✅ |
| **অ্যাডমিন রোল** | is_admin() ফাংশন | ✅ |
| **ক্রেডেনশিয়াল এনক্রিপশন** | AES-GCM এনক্রিপশন | ✅ |
| **ইমেইল ভেরিফিকেশন** | সাইনআপ ভেরিফিকেশন | ✅ |
| **গেস্ট চেকআউট সিকিউরিটি** | ফ্রড প্রোটেকশন | ✅ |

### 🌐 লোকালাইজেশন

| ফিচার | বর্ণনা | স্ট্যাটাস |
|--------|--------|----------|
| **ডুয়াল ল্যাঙ্গুয়েজ** | English / বাংলা | ✅ |
| **RTL সাপোর্ট** | বাংলা টেক্সট | ✅ |
| **বাংলা ফন্ট** | হিন্দ সিলিগুরি | ✅ |
| **কন্টেন্ট ট্রান্সলেশন** | ডাটাবেস-লেভেল | ✅ |

---

## ডাটাবেস আর্কিটেকচার

### প্রধান টেবিল গ্রুপ

```
┌─────────────────────────────────────────────────────────────┐
│                    PRODUCT MANAGEMENT                        │
├─────────────────────────────────────────────────────────────┤
│ products ──┬── product_variants ──┬── product_colors        │
│            │                      └── product_sizes          │
│            ├── categories                                    │
│            ├── collections                                   │
│            └── product_bundles ── bundle_products           │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    ORDER MANAGEMENT                          │
├─────────────────────────────────────────────────────────────┤
│ orders ──┬── order_items                                    │
│          ├── addresses                                       │
│          ├── delivery_zones                                  │
│          └── delivery_partners                               │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    USER MANAGEMENT                           │
├─────────────────────────────────────────────────────────────┤
│ auth.users ──┬── profiles                                   │
│              ├── user_roles (admin access)                  │
│              ├── customers (CRM data)                       │
│              ├── cart_items                                  │
│              └── wishlist_items                              │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    CMS & SETTINGS                            │
├─────────────────────────────────────────────────────────────┤
│ hero_slides, homepage_sections, homepage_content            │
│ blog_posts, blog_categories, faq_items                      │
│ testimonials, gallery_albums, gallery_items                 │
│ site_branding, theme_settings, shop_settings               │
│ checkout_settings, email_settings, invoice_settings        │
└─────────────────────────────────────────────────────────────┘
```

### ডাটাবেস ফাংশন

```sql
-- অ্যাডমিন চেক ফাংশন
public.is_admin(check_user_id uuid) → boolean

-- অর্ডার নম্বর জেনারেট
public.generate_order_number() → text

-- কাস্টমার ডাটা সিঙ্ক
public.sync_customer_on_order() → trigger
```

---

## অথেন্টিকেশন সিস্টেম

### ইউজার রোল

```typescript
// useAuth.tsx - অথ স্টেট ম্যানেজমেন্ট
const { user, isLoading, signIn, signUp, signOut } = useAuth();

// useAdmin.tsx - অ্যাডমিন চেক
const { isAdmin, isLoading, role } = useAdmin();
```

### RLS পলিসি প্যাটার্ন

```sql
-- অ্যাডমিন-ওনলি টেবিল
CREATE POLICY "Admin only access"
ON public.orders FOR ALL
USING (public.is_admin(auth.uid()));

-- ইউজার-ওন ডাটা
CREATE POLICY "Users own data"
ON public.cart_items FOR SELECT
USING (auth.uid() = user_id);
```

---

## রিয়েল-টাইম সিঙ্ক

### সাবস্ক্রিপশন প্যাটার্ন

```typescript
useEffect(() => {
  const channel = supabase
    .channel('orders_realtime')
    .on(
      'postgres_changes',
      { event: '*', schema: 'public', table: 'orders' },
      (payload) => {
        fetchData(); // Refetch on change
      }
    )
    .subscribe();

  return () => {
    supabase.removeChannel(channel);
  };
}, []);
```

### রিয়েল-টাইম এনেবলড টেবিল

- `orders` - অর্ডার আপডেট
- `products` - প্রোডাক্ট চেঞ্জ
- `cart_items` - কার্ট সিঙ্ক
- `homepage_sections` - CMS আপডেট
- `site_branding` - ব্র্যান্ডিং চেঞ্জ

---

## Edge Functions

### উপলব্ধ ফাংশন

| ফাংশন | পাথ | বর্ণনা |
|--------|-----|--------|
| `bkash-payment` | `/bkash-payment` | bKash পেমেন্ট প্রসেস |
| `nagad-payment` | `/nagad-payment` | Nagad পেমেন্ট প্রসেস |
| `send-order-email` | `/send-order-email` | অর্ডার ইমেইল |
| `generate-invoice` | `/generate-invoice` | PDF ইনভয়েস |
| `generate-delivery-slip` | `/generate-delivery-slip` | ডেলিভারি স্লিপ |
| `encrypt-credentials` | `/encrypt-credentials` | ক্রেডেনশিয়াল এনক্রিপশন |
| `fetch-google-reviews` | `/fetch-google-reviews` | গুগল রিভিউ |

### Edge Function কল

```typescript
const { data, error } = await supabase.functions.invoke('send-order-email', {
  body: { orderId: 'uuid', type: 'confirmation' }
});
```

---

## স্টাইলিং ও থিম

### ডিজাইন টোকেন

```css
/* index.css */
:root {
  --gold: 45 100% 51%;
  --gold-light: 45 100% 60%;
  --gold-dark: 45 100% 40%;
  --charcoal: 240 5% 10%;
  --charcoal-deep: 240 5% 6%;
  --cream: 45 100% 96%;
  --background: 240 5% 4%;
  --foreground: 45 100% 98%;
}
```

### কম্পোনেন্ট ভ্যারিয়েন্ট

```typescript
// button.tsx
variant: "gold" | "gold-outline" | "hero" | "hero-outline"
```

---

## লোকালাইজেশন

### ল্যাঙ্গুয়েজ হুক

```typescript
const { language, setLanguage, t } = useLanguage();

// ব্যবহার
<p>{language === "bn" ? "বাংলা টেক্সট" : "English text"}</p>
```

### ডাটাবেস ফিল্ড

```sql
-- বাংলা ভার্সন সাপোর্ট
title TEXT NOT NULL,
title_bn TEXT,
description TEXT,
description_bn TEXT,
```

---

## ডিপ্লয়মেন্ট

### Lovable Cloud (বর্তমান)

```bash
# অটোমেটিক ডিপ্লয়মেন্ট
# Preview: https://id-preview--project-id.lovable.app
# Production: Publish বাটন ক্লিক
```

### অন্য প্ল্যাটফর্মে

দেখুন: [DATABASE_MIGRATION_GUIDE.md](./DATABASE_MIGRATION_GUIDE.md)

---

## ডেভেলপমেন্ট কমান্ড

```bash
# লোকাল ডেভেলপমেন্ট
npm run dev

# বিল্ড
npm run build

# টেস্ট
npm run test

# লিন্ট
npm run lint
```

---

## গুরুত্বপূর্ণ ফাইল

| ফাইল | বর্ণনা |
|------|--------|
| `src/integrations/supabase/client.ts` | Supabase ক্লায়েন্ট (auto-generated) |
| `src/integrations/supabase/types.ts` | ডাটাবেস টাইপস (auto-generated) |
| `src/hooks/useAuth.tsx` | অথেন্টিকেশন হুক |
| `src/hooks/useAdmin.tsx` | অ্যাডমিন চেক হুক |
| `src/hooks/useCart.tsx` | কার্ট ম্যানেজমেন্ট |
| `src/hooks/useCustomizationSettings.tsx` | কাস্টমাইজেশন সেটিংস হুক |
| `src/pages/Admin.tsx` | অ্যাডমিন ড্যাশবোর্ড রাউট |
| `src/components/product/ProductDiscountBadge.tsx` | ডাইনামিক ডিসকাউন্ট ব্যাজ |
| `src/components/modals/ProductCustomOrderModal.tsx` | কাস্টম অর্ডার মোডাল |
| `src/components/admin/AdminCustomizationSettings.tsx` | কাস্টমাইজেশন অ্যাডমিন সেটিংস |
| `tailwind.config.ts` | থিম কনফিগারেশন |
| `index.css` | গ্লোবাল স্টাইল ও CSS ভেরিয়েবল |

---

## নতুন ফিচার: কাস্টমাইজেশন অনলি প্রোডাক্ট

### কনসেপ্ট
যেসব প্রোডাক্ট সরাসরি কার্টে যোগ করা যায় না, শুধু কাস্টম অর্ডার রিকোয়েস্ট করতে হয়।

### ডাটাবেস ফিল্ড
```sql
-- products টেবিলে নতুন ফিল্ড
customization_only BOOLEAN DEFAULT false
advance_payment_percent INTEGER DEFAULT 50
customization_instructions TEXT
```

### ব্যবহার

```typescript
// প্রোডাক্ট ডিটেইল পেজে
if (product.customization_only) {
  // "Request Custom Order" বাটন দেখাবে
  // "Add to Cart" ও "Buy Now" হাইড থাকবে
}
```

---

## হেডার বাটন ও ফর্ম কাস্টমাইজেশন

### `customization_settings` টেবিল (সম্পূর্ণ ফিল্ড লিস্ট)

| ফিল্ড | টাইপ | বর্ণনা | ডিফল্ট |
|-------|------|--------|--------|
| `custom_order_enabled` | BOOLEAN | কাস্টম অর্ডার সিস্টেম চালু | true |
| `header_button_enabled` | BOOLEAN | হেডারে বাটন দেখানো | true |
| `header_button_text` | VARCHAR(100) | বাটন টেক্সট (ইংরেজি) | "Custom Design" |
| `header_button_text_bn` | VARCHAR(100) | বাটন টেক্সট (বাংলা) | "কাস্টম ডিজাইন" |
| `header_button_link` | VARCHAR(500) | কাস্টম লিঙ্ক (null = modal) | null |
| `header_button_icon` | VARCHAR(50) | Lucide আইকন নাম | "Palette" |
| `default_advance_percent` | INTEGER | ডিফল্ট অ্যাডভান্স % | 50 |
| `min_advance_percent` | INTEGER | মিনিমাম অ্যাডভান্স % | 20 |
| `max_advance_percent` | INTEGER | ম্যাক্সিমাম অ্যাডভান্স % | 100 |
| `form_title` | VARCHAR(200) | ফর্ম টাইটেল (ইংরেজি) | "Submit Your Design" |
| `form_title_bn` | VARCHAR(200) | ফর্ম টাইটেল (বাংলা) | "আপনার ডিজাইন জমা দিন" |
| `form_subtitle` | VARCHAR(500) | ফর্ম সাবটাইটেল (ইংরেজি) | "Upload your design idea..." |
| `form_subtitle_bn` | VARCHAR(500) | ফর্ম সাবটাইটেল (বাংলা) | "আপনার ডিজাইন আইডিয়া আপলোড করুন..." |
| `form_description_label` | VARCHAR(200) | ডেসক্রিপশন লেবেল | "Detailed Description" |
| `form_description_placeholder` | VARCHAR(500) | ডেসক্রিপশন প্লেসহোল্ডার | "Describe your preferred colors..." |
| `require_image` | BOOLEAN | ছবি বাধ্যতামূলক | false |
| `show_budget_fields` | BOOLEAN | বাজেট ফিল্ড দেখানো | true |
| `success_message` | VARCHAR(500) | সফল মেসেজ (ইংরেজি) | "Your custom order request..." |
| `success_message_bn` | VARCHAR(500) | সফল মেসেজ (বাংলা) | "আপনার কাস্টম অর্ডার রিকোয়েস্ট..." |

### কাস্টমাইজেশন হুক

```typescript
import { useCustomizationSettings } from "@/hooks/useCustomizationSettings";

const { settings, loading, updateSettings, refetch } = useCustomizationSettings();

// সেটিংস অ্যাক্সেস
if (settings?.header_button_enabled) {
  // বাটন দেখাও
}
```

### অ্যাডমিন প্যানেল অ্যাক্সেস

```
Admin Dashboard → Settings → Custom Order Settings
```

তিনটি ট্যাব:
1. **General**: ফিচার টগল, হেডার বাটন কাস্টমাইজ
2. **Form Customization**: ফর্ম টাইটেল, লেবেল, প্লেসহোল্ডার, ভ্যালিডেশন
3. **Payment**: অ্যাডভান্স পেমেন্ট শতাংশ সেটিংস

### দুইটি মোডাল

| মোডাল | ব্যবহার | কম্পোনেন্ট |
|-------|---------|-----------|
| `CustomOrderModal` | হেডার বাটন থেকে ওপেন হয় | সাধারণ ডিজাইন রিকোয়েস্ট |
| `ProductCustomOrderModal` | প্রোডাক্ট ডিটেইল থেকে ওপেন হয় | প্রোডাক্ট-স্পেসিফিক কাস্টম অর্ডার |

### কাস্টম অর্ডার ফ্লো

```
┌─────────────────────────────────────────────────────────────┐
│                    HEADER BUTTON FLOW                        │
├─────────────────────────────────────────────────────────────┤
│ হেডার বাটন ক্লিক → CustomOrderModal →                       │
│ রেফারেন্স ইমেজ + ডেসক্রিপশন + বাজেট → সাবমিট              │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                  PRODUCT-SPECIFIC FLOW                       │
├─────────────────────────────────────────────────────────────┤
│ প্রোডাক্ট "Request Custom Order" ক্লিক →                    │
│ ProductCustomOrderModal → ৩ স্টেপ ফ্লো:                     │
│   Step 1: রিকোয়ারমেন্ট (ইমেজ, ডেসক্রিপশন)                  │
│   Step 2: ডেলিভারি (নাম, ফোন, ঠিকানা)                       │
│   Step 3: পেমেন্ট (bKash/Nagad/COD)                         │
│ → অ্যাডভান্স পেমেন্ট কমপ্লিট → অ্যাডমিন রিভিউ              │
└─────────────────────────────────────────────────────────────┘
```

---

## সম্পর্কিত ফাইল

| ফাইল | বর্ণনা |
|------|--------|
| `src/hooks/useCustomizationSettings.tsx` | সেটিংস ফেচ ও আপডেট |
| `src/components/modals/CustomOrderModal.tsx` | হেডার বাটন মোডাল |
| `src/components/modals/ProductCustomOrderModal.tsx` | প্রোডাক্ট কাস্টম অর্ডার |
| `src/components/admin/AdminCustomizationSettings.tsx` | অ্যাডমিন সেটিংস পেজ |
| `src/components/layout/Header.tsx` | হেডার বাটন রেন্ডার |

---

*শেষ আপডেট: ২০২৬-০২-০৯*
