import CMSPage from "@/components/content/CMSPage";

const ReturnPolicy = () => {
  return (
    <CMSPage
      pageKey="return-policy"
      eyebrow="Policy"
      fallbackTitle="Return Policy"
      fallbackDescription="Understand our return and refund policies for your purchases."
    />
  );
};

export default ReturnPolicy;
